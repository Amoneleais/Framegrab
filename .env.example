# Example environment variables for local development
# Copy this file to .env and adjust values before running the app.

# Postgres connection string. When using Docker compose the service name is `db`.
DATABASE_URL=postgresql://postgres:postgres@db:5432/framegrab?schema=public

MOVIES_PATH=/path/to/your/movies